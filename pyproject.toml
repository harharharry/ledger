[project]
name = "ledger"
version = "0.1.0"
description = "Personal paper-first investment assistant — a discipline and learning tool, not a market-prediction system"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
